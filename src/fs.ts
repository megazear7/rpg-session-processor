import path from "path";

export const audioPath = path.join(process.cwd(), 'data', 'audio.mp3');
export const storyPath = path.join(process.cwd(), 'output', 'story.txt');
export const summaryPath = path.join(process.cwd(), 'output', 'summary.txt');
